import PageHeader from '../../components/PageHeader'
import styles from './index.module.scss'

function LocalizationRecord () {
  return (
    <div className={styles.whole}>
      <PageHeader title="定位记录" />
    </div>
  )
}

export default LocalizationRecord
